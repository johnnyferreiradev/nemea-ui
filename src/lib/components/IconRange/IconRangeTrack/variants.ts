import { tv } from 'tailwind-variants';

export const iconRangeTrackVariants = tv({
  base: [
    'au-icon-range-track',
    'relative flex-grow rounded-full overflow-hidden',
    'bg-[var(--grayscale-100)]',
    'dark:bg-[var(--grayscale-400)]',
    'h-[32px]',
  ],
});
