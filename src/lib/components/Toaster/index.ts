import ToasterRoot from './ToasterRoot';
import ToasterTitle from './ToasterTitle';
import ToasterDescription from './ToasterDescription';
import ToasterAction from './ToasterAction';
import ToasterClose from './ToasterClose';
import ToasterIcon from './ToasterIcon';
import ToasterContent from './ToasterContent';
import ToasterActions from './ToasterActions';

export const Toaster = {
  Root: ToasterRoot,
  Title: ToasterTitle,
  Description: ToasterDescription,
  Action: ToasterAction,
  Close: ToasterClose,
  Icon: ToasterIcon,
  Content: ToasterContent,
  Actions: ToasterActions,
};
